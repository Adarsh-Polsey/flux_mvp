import 'package:flutter/material.dart';

class ViewSavedCards extends StatelessWidget {
  const ViewSavedCards({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}